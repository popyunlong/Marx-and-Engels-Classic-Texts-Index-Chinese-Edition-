# 安装到 /etc/systemd/journald.conf.d/marx-search.conf，再 systemctl restart systemd-journald。
# 给 journald 设上限与保留期：保证应用 management_action / 自动封禁 / auth_failure 等审计
# 至少能回溯约 30 天，且不会无限占盘。
[Journal]
SystemMaxUse=500M
MaxRetentionSec=30day
